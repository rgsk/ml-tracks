practice/
.venv
__pycache__
*.egg-info/
checkpoints/

# generated files: training checkpoints + tokenizer cache (rebuilt on demand)
artifacts/
outputs/

# downloaded datasets (re-fetched on demand), e.g. diffusion/data/mnist.npz
data/
*.npz
nb/llm/data/input.txt

# jupytext-paired notebooks: the .py is source of truth, the .ipynb is regenerated
# with `jupytext --to ipynb --execute` (outputs not committed). Covers walkthroughs/
# and custom/ (from-scratch impls are paired too, for LaTeX/figures in their write-ups).
nb/**/walkthroughs/*.ipynb
nb/**/custom/*.ipynb
nb/**/rough/*.ipynb