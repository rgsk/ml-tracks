If it started at, say, 50, you'd have a bug (bad init, wrong reshape, exploding logits). If it started at 0.1, your targets are leaking into the input. 
explain this with example


Weights & Biases


understand gqa tests

match closed_form and iterative at each t

LEFT: SEE the spread widen as we scale. Wider box = bigger variance. in new/diffusion/foundations/gaussian_facts/walkthroughs/linear_ops.py


import torch
x = torch.randn(2000, 64)
y = torch.randn(64, 2000)
z = x @ y
print(z.var())
z = x @ y * 64 ** -.5
print(z.var())